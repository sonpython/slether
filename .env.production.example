# Cloudflare Tunnel
# Get token from: https://one.dash.cloudflare.com → Networks → Tunnels → Create → select Docker
CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here

# GitHub Actions Runner
# Get token from: GitHub repo → Settings → Actions → Runners → New self-hosted runner
GITHUB_RUNNER_TOKEN=your-runner-token-here
GITHUB_REPO_URL=https://github.com/sonpython/slether
GITHUB_RUNNER_NAME=slether-runner
